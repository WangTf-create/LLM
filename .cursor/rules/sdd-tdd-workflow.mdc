---
description: 待办事项项目 SDD + TDD 工作模式（先 Spec，后测试，再实现）
alwaysApply: true
---

# SDD + TDD 工作模式

## 核心原则

1. **先 Spec，后实现** — 任何功能必须先探讨/修订 spec，经人确认后再写代码
2. **测试必积累** — 所有产出都要对应测试用例；测试是 AI 的退出标准

## 标准流程

```
Spec 探讨 → 人确认 → 写测试（红）→ 写实现（绿）→ 重构（保持绿）
```

- Spec 位置：`code/backend/spec/*.md`
- 一个端点一个循环，不要一次写完整个服务
- spec 未写清的规则，回到 spec 讨论，不边写边猜

## Spec 要求

- 包含：数据模型、端点、边界（每条会变成测试）、明确不做
- 人能在 2 分钟内审完
- 每条规则必须能写成测试

## TDD 要求

- 正常路径：每个端点至少 1 条测试
- 边界：spec「边界」小节每条 1 条测试
- 红 → 绿 → 重构；不许注释掉失败测试
- 改了行为就更新/补测试
- 测试失败时反馈具体断言 + 输入，不只说「有 bug」

## 前端

- 后端 OpenAPI 契约（`openapi.json`）是前端 spec
- UI 约束从契约继承，不自行编造后端不存在的字段

## 禁止

- 跳过 spec 直接写实现
- 无对应测试的功能交付
- 引入未在 requirements 里的新依赖（先问）
